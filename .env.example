#!/bin/bash
# Supabase Storage Configuration
# Configure these values from your Supabase project (https://app.supabase.com)
# See docs/SUPABASE_SETUP.md for detailed setup instructions
# 
# WARNING: Never commit .env file to git! Use .env.example as reference.
# The .env file is in .gitignore to prevent accidental secret exposure.

export SUPABASE_URL="https://your-project-id.supabase.co"
export SUPABASE_PROJECT_ID="your-project-id"
export SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"
export SUPABASE_BUCKET="materials"
